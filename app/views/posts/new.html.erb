<div class="container-fluid">
<%= form_for(@post, url: posts_path) do |f| %>
    <label>
        titre 
    </label>
    <%= f.text_field :title %>
    <label>
        Contenu
    </label>
    <%= f.text_area :content %>
    <%= f.submit %>
<% end %>
</div>